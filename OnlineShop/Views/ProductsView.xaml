<UserControl x:Class="OnlineShop.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer>
        <WrapPanel Margin="20">
            <ItemsControl ItemsSource="{Binding}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="280" 
                                Height="400" 
                                Margin="10" 
                                BorderBrush="#E0E0E0" 
                                BorderThickness="1"
                                Background="White"
                                CornerRadius="5">
                            <StackPanel>
                                <Border Height="180" 
                                        Background="#F8F9FA">
                                    <Image Source="{Binding ImageUrl}" 
                                           Stretch="Uniform"/>
                                </Border>

                                <StackPanel Margin="15" Height="150">
                                    <TextBlock Text="{Binding Name}" 
                                               FontWeight="Bold" 
                                               FontSize="16"
                                               TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Description}" 
                                               Foreground="#666" 
                                               FontSize="12"
                                               TextWrapping="Wrap"
                                               Margin="0,5,0,0"/>
                                    <TextBlock FontWeight="Bold" FontSize="18" Foreground="#2C3E50" Margin="0,10,0,0">
                                        <Run Text="Цена: "/>
                                        <Run Text="{Binding Price}"/>
                                        <Run Text=" руб."/>
                                    </TextBlock>
                                </StackPanel>

                                <Button Content="Добавить в корзину" 
                                        Tag="{Binding}"
                                        Click="AddToCart_Click"
                                        Background="#3498DB" 
                                        Foreground="White"
                                        BorderThickness="0"
                                        Height="40"
                                        FontSize="14"
                                        Cursor="Hand"
                                        Margin="10">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#2980B9"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </WrapPanel>
    </ScrollViewer>
</UserControl>